# ============================================
# Hammam System - Environment Variables
# ============================================
# Copy this file to .env and fill in the values
# cp .env.example .env
#
# NEVER commit .env to Git!
# ============================================

# PostgreSQL password (use a strong password)
POSTGRES_PASSWORD=ChangeMe_StrongPassword123!

# JWT Secret (minimum 256 bits / 32 characters)
# Generate one with: openssl rand -base64 64
JWT_SECRET=ChangeMe_GenerateASecureJwtSecretKeyWith64Characters!!

# Allowed CORS origins (comma-separated)
# Set to your domain in production
CORS_ORIGINS=http://localhost,http://your-domain.com,https://your-domain.com
